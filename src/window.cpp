#include "window.h"

Window::Window(QMainWindow *parent) : QMainWindow(parent)
{
    // widgets

    // properties
    resize(250, 250);
    setWindowTitle("Eddy 0.1");

    // layouts

    // connections
}