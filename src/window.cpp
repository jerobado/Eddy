#include "include/window.h"

Window::Window(QWidget *parent) : QWidget(parent)
{
    // widgets

    // properties
    resize(250, 250);
    setWindowTitle("Eddy 0.1");

    // layouts

    // connections
}